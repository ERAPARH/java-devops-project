apiVersion: v1
kind: Service
metadata:
  name: java-devops-project-service
spec:
  selector:
    app: java-devops-project
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
